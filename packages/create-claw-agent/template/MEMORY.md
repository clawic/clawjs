# Memory

Base memory for __APP_TITLE__.

Stable facts:

- this repository is agent-first
- runtime defaults to `demo`
- the first operator workflow is inspect -> initialize -> report -> reply

Working memory areas:

- user preferences
- recurring tasks
- unresolved blockers
- useful reusable prompts
