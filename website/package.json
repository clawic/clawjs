{
  "name": "@clawjs/website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:docs": "node ../scripts/sync-website-docs.mjs",
    "dev": "npm run sync:docs && vite",
    "build": "npm run sync:docs && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
